{
  "name": "wallet-e2e",
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "outputs": ["{options.outputPath}"],
      "options": {
        "command": "npx playwright test --config apps/wallet-e2e/playwright.config.ts",
        "outputPath": "dist/apps/wallet-e2e"
      }
    }
  }
}
